import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks";
import * as Stories from "./JBColorInput.stories";

<Meta of={Stories} />

# Color Input

`jb-color-input` combines the standard form, validation, and size behavior of `jb-input` with `jb-color-picker` in a popover.

<Canvas of={Stories.Normal} />
<Controls of={Stories.Normal} />

