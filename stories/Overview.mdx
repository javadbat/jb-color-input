import { Meta, Title, Story, Controls, Stories, Source } from "@storybook/addon-docs/blocks";
import * as ColorInputStories from "./JBColorInput.stories";

<Meta of={ColorInputStories} />

<Title>JB Color Input</Title>

Form-associated CSS color input with the standard `jb-input` label, validation, form, and size behavior plus `jb-color-picker` in a popover.

<Story of={ColorInputStories.Normal} />
<Controls of={ColorInputStories.Normal} />
<Source of={ColorInputStories.Normal} />

## Size variants

The input and its color trigger support `xs`, `sm`, `md`, `lg`, and `xl` sizes.

<Story of={ColorInputStories.SizeVariants} />

## Disabled state

Disabled state prevents text editing and picker interaction.

<Story of={ColorInputStories.Disabled} />

## Validation

Non-empty values must use a supported RGB, hexadecimal, or OKLCH CSS color syntax.

<Story of={ColorInputStories.InvalidColor} />

## Other samples and variants

<Stories />
